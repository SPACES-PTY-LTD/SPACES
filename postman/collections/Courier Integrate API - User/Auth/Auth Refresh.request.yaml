$kind: http-request
url: "{{base_url}}/api/v1/auth/refresh"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "refresh_token": "{{refresh_token}}"
    }
order: 4000
