$kind: http-request
url: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_vehicles"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "merchant_id": "{{merchant_id}}"
    }
order: 3000
