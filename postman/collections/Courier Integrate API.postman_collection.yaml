---
info:
  _postman_id: 724db142-d437-4c25-8c68-b5c106e4f280
  name: Courier Integrate API - User
  schema: https://schema.getpostman.com/json/collection/v3.0.0/collection.json
item:
- name: Auth
  item:
  - name: Auth Register
    id: f8b87f3c-955a-4f84-a66f-3f24a40c10d9
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "name": "Jane Doe",
            "email": "jane@example.com",
            "password": "password123",
            "password_confirmation": "password123"
          }
      url:
        raw: "{{base_url}}/api/v1/auth/register"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - auth
        - register
    response: []
  - name: Auth Login
    event:
    - listen: test
      script:
        id: 9a40f5c3-742c-4175-98f8-f961bf5477c6
        exec:
        - "// Parse the JSON response"
        - const jsonData = pm.response.json();
        - ''
        - "// Extract token from common response locations"
        - let token = null;
        - ''
        - if (jsonData.data.token) {
        - "    token = jsonData.data.token;"
        - "}"
        - ''
        - "// Save token to environment variable"
        - if (token) {
        - '    pm.environment.set("bearer_token", token);'
        - '    console.log("Token saved to environment variable ''bearer_token''");'
        - "}"
        - ''
        - "// Test to verify token was received"
        - pm.test("Token is present in response", function () {
        - "    pm.expect(token).to.not.be.null;"
        - "    pm.expect(token).to.be.a('string');"
        - "    pm.expect(token.length).to.be.greaterThan(0);"
        - "});"
        type: text/javascript
        packages: []
        requests: []
    - listen: prerequest
      script:
        id: 4b23f820-a2e5-4c88-999f-aabb860e7622
        exec:
        - ''
        type: text/javascript
        packages: []
        requests: []
    id: 2da7b7a0-8948-49b2-8c09-8bedb5b170dc
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "email": "normal_admin@example.com",
            "password": "password1234"
          }
      url:
        raw: "{{base_url}}/api/v1/auth/login"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - auth
        - login
    response: []
  - name: Auth Logout
    id: 4a046256-4738-48ab-91c2-0b4b53f0ef74
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/auth/logout"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - auth
        - logout
    response: []
  - name: Auth Refresh
    id: 6f8f61f5-53a6-4261-ae7c-6b2a5bed6182
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "refresh_token": "{{refresh_token}}"
          }
      url:
        raw: "{{base_url}}/api/v1/auth/refresh"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - auth
        - refresh
    response: []
  id: e9eb9994-3ab1-491e-b6c2-c4659ad5e3fe
- name: Me
  item:
  - name: Get Me
    id: 8124fc54-63d9-410a-af18-f93df52d69ab
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/me"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - me
    response: []
  id: 8eb8d123-7669-4423-b8d9-0c7db6052976
- name: Merchants
  item:
  - name: Create Merchant
    id: 86b8f1d9-c456-48e7-95e7-a55ee9baa541
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "name": "Acme Logistics"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
    response: []
  - name: List Merchants
    id: 47d09a33-d0a8-4b97-bb9d-ab6cd3c6bc30
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
    response: []
  - name: Get Merchant
    id: ce7898f5-417a-44d7-be79-ae8a4e40f175
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
    response: []
  - name: Update Merchant
    id: 6f81f676-b7a5-409f-bd98-59ab85afd742
    request:
      method: PATCH
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "name": "Acme Logistics Updated"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
    response: []
  - name: Delete Merchant
    id: d88ffa51-cd5e-48ad-8954-f82abcdc6d62
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
    response: []
  id: 7cb286e9-a3b8-47e7-9f04-152cf111a0f0
- name: Merchant Members
  item:
  - name: Invite Member
    id: 1934af8c-f1d3-416f-b306-e2412aa96897
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "email": "member@example.com",
            "role": "member"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/members/invite"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - members
        - invite
    response: []
  - name: List Members
    id: 779e882a-58d4-4ee0-9538-5cdec9164138
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/members"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - members
    response: []
  - name: Update Member
    id: c1f7a228-67bc-4abd-904a-c55828ca1489
    request:
      method: PATCH
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "role": "admin"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/members/{{user_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - members
        - "{{user_id}}"
    response: []
  - name: Remove Member
    id: ece8ae53-27cb-41e3-91cc-6b487c6d5eb4
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/members/{{user_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - members
        - "{{user_id}}"
    response: []
  id: 8293e7fe-424c-4c66-95f2-efaf89a79226
- name: Merchant Environments
  item:
  - name: List Merchant Environments
    id: 9aed0d57-5e95-42d3-b5d8-2b101f2f80a3
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
    response: []
  - name: Create Merchant Environment
    id: 80010c66-d3bf-43f9-937a-a96cdbf41d96
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |
          {
            "name": "Production",
            "color": "#1F2937"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
    response: []
  - name: Get Merchant Environment
    id: 94840791-6273-49a7-a77c-a11a673323bb
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments/{{environment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
        - "{{environment_id}}"
    response: []
  - name: Update Merchant Environment
    id: 189cbb46-0b91-42e0-8bf3-86eec9659a1b
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "name": "Production Updated"
          }
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments/{{environment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
        - "{{environment_id}}"
    response: []
  - name: Delete Merchant Environment
    id: 9a23638d-2a34-40e5-a938-4dbadb64aa85
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments/{{environment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
        - "{{environment_id}}"
    response: []
  - name: Rotate Merchant Environment Token
    id: be442a4b-05ff-44c1-931f-9e0833e357a5
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/environments/{{environment_id}}/token"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - environments
        - "{{environment_id}}"
        - token
    response: []
  id: 9bf66bb6-15f7-4310-8cad-355ccd7067b7
- name: Merchant Invites
  item:
  - name: Accept Invite (Public)
    id: e09a36ca-9108-4d94-b13a-45a396187c3f
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "invite_token": "your-invite-token",
            "password": "password123",
            "password_confirmation": "password123"
          }
      url:
        raw: "{{base_url}}/api/v1/merchant-invites/accept"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchant-invites
        - accept
    response: []
  - name: List Invites
    id: d012543b-3756-4f5d-9459-268ab0d5feba
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/invites"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - invites
    response: []
  - name: Resend Invite
    id: 4f71ec3b-9cc0-44cb-a2a6-72a1f590f176
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/invites/{{invite_id}}/resend"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - invites
        - "{{invite_id}}"
        - resend
    response: []
  - name: Revoke Invite
    id: f53bbf30-4185-4e3e-9b86-2f39f309e478
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_id}}/invites/{{invite_id}}/revoke"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_id}}"
        - invites
        - "{{invite_id}}"
        - revoke
    response: []
  id: 3a5450eb-e396-40cb-906f-79b48fd354ca
- name: Quotes
  item:
  - name: Get Quotes
    id: b12b8acb-9350-4399-9775-9fd7d9639bf1
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/quote"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - quote
      description: Returns `selected_option` when the quote status is `booked`.
    response: []
  - name: Request Quote
    id: e2a4f7f1-001f-4c91-92c8-00d3bb95a124
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
              "merchant_id": "{{merchant_uuid}}",
              "collection_date": "2026-02-14T09:00:00Z",
              "pickup_address": {
                  "name": "Warehouse A",
                  "code": "WH-A",
                  "company": "PicknDrop",
                  "address_line_1": "6 Woodmere Road",
                  "address_line_2": "Sunningdale",
                  "town": "Sunningdale",
                  "city": "Cape Town",
                  "province": "Western Cape",
                  "post_code": "7441",
                  "country": "ZA",
                  "first_name": "John",
                  "last_name": "Doe",
                  "phone": "+27-21-555-1234",
                  "latitude": -33.7801,
                  "longitude": 18.4567,
                  "google_place_id": "ChIJN1t_tDeuEmsRUsoyG83frY4"
              },
              "dropoff_address": {
                  "name": "Customer",
                  "code": "CUST-1001",
                  "company": "Customer Co",
                  "address_line_1": "1 Woodmere Road",
                  "address_line_2": "Sunningdale",
                  "town": "Sunningdale",
                  "city": "Cape Town",
                  "province": "Western Cape",
                  "post_code": "7441",
                  "country": "ZA",
                  "first_name": "Jane",
                  "last_name": "Smith",
                  "phone": "+27-21-555-5678",
                  "latitude": -33.7812,
                  "longitude": 18.4588,
                  "google_place_id": "ChIJrTLr-GyuEmsRBfy61i59si0"
              },
              "parcels": [
                  {
                      "title": "ABC",
                      "weight_kg": 1,
                      "length_cm": 10,
                      "width_cm": 10,
                      "height_cm": 10
                  }
              ]
          }
      url:
        raw: "{{base_url}}/api/v1/quotes"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - quotes
    response: []
  - name: Get Quote
    id: 06e84433-75b1-43e5-ad57-71a7ff4dc9fb
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/quotes/{{quote_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - quotes
        - "{{quote_id}}"
      description: Returns `selected_option` when the quote status is `booked`.
    response: []
  - name: List Shipment Quotes
    id: 3b7d5bbb-ee58-46cd-a75c-069cdded2acb
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}/quotes"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
        - quotes
    response: []
  id: c73d90c9-f1c0-41a7-b16e-4358b36c56ad
- name: Shipments
  item:
  - name: Create Shipment
    id: '086fd047-38f3-4f53-bab9-53eabe7f30e2'
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_uuid}}",
            "merchant_order_ref": "ORDER-1234",
            "collection_date": "2026-02-14T09:00:00Z",
            "pickup_address": {
              "name": "Warehouse A",
              "code": "WH-A",
              "company": "PicknDrop",
              "address_line_1": "6 Woodmere Road",
              "address_line_2": "Sunningdale",
              "town": "Sunningdale",
              "city": "Cape Town",
              "province": "Western Cape",
              "post_code": "7441",
              "country": "ZA",
              "first_name": "John",
              "last_name": "Doe",
              "phone": "+27-21-555-1234",
              "latitude": -33.7801,
              "longitude": 18.4567,
              "google_place_id": "ChIJN1t_tDeuEmsRUsoyG83frY4"
            },
            "dropoff_address": {
              "name": "Customer",
              "code": "CUST-1001",
              "company": "Customer Co",
              "address_line_1": "1 Woodmere Road",
              "address_line_2": "Sunningdale",
              "town": "Sunningdale",
              "city": "Cape Town",
              "province": "Western Cape",
              "post_code": "7441",
              "country": "ZA",
              "first_name": "Jane",
              "last_name": "Smith",
              "phone": "+27-21-555-5678",
              "latitude": -33.7812,
              "longitude": 18.4588,
              "google_place_id": "ChIJrTLr-GyuEmsRBfy61i59si0"
            },
            "parcels": [
              {
                "weight_kg": 1,
                "length_cm": 10,
                "width_cm": 10,
                "height_cm": 10
              }
            ]
          }
      url:
        raw: "{{base_url}}/api/v1/shipments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
    response: []
  - name: List Shipments
    id: 9d3b36dd-6062-46ea-a2d7-e08530d22d5c
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
    response: []
  - name: Get Shipment
    id: 12b85190-00dc-4e31-8a24-1dc3e6fe7870
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
    response: []
  - name: Update Shipment
    id: a85497cf-e09a-4cd3-8667-372c9d8cfeaf
    request:
      method: PATCH
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "collection_date": "2026-02-15T09:00:00Z"
          }
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
    response: []
  - name: Delete Shipment
    id: cb183b00-4de3-42d7-808f-f190af1ae86f
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
    response: []
  - name: Get Shipment Label
    id: 345d5683-340e-4d1b-b959-afd5984e07ad
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}/label"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
        - label
    response: []
  - name: Get Shipment Tracking
    id: ac75a72b-1560-4418-bdbe-3142e0378e6e
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}/tracking"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
        - tracking
    response: []
  id: 9f04d7c9-527a-47e9-88e8-59d8930db788
- name: Bookings
  item:
  - name: Book Shipment
    id: 75e7f1c0-3677-475f-b788-8d61d3d86413
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      - key: Idempotency-Key
        value: idem-1
        disabled: true
      body:
        mode: raw
        raw: |-
          {
            "quote_option_id": "{{quote_option_id}}"
          }
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}/book"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
        - book
    response: []
  - name: List Bookings
    id: a6234f58-9a6c-46b2-95d0-70f0dc1f5d3f
    request:
      method: GET
      header: []
      url:
        raw: "{{base_url}}/api/v1/bookings"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - bookings
    response: []
  - name: Get Booking
    id: 0df2edef-d148-4f2f-aa79-9226000d3923
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/bookings/{{booking_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - bookings
        - "{{booking_id}}"
    response: []
  - name: Cancel Shipment
    id: 4ca6199e-8d67-4c12-b064-a022cf5b0924
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_id}}/cancel"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_id}}"
        - cancel
    response: []
  id: b7f44994-4b39-4a3e-a5b5-202c2060e544
- name: Webhook Subscriptions
  item:
  - name: Create Subscription
    id: b49e3f75-17d9-49e0-899d-5144b252fb87
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "url": "https://example.com/webhook",
            "event": "shipment.updated"
          }
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
    response: []
  - name: List Subscriptions
    id: 053ebb41-66ee-48c0-bf11-af9bb3260881
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
    response: []
  - name: Delete Subscription
    id: b8b4ab7c-efd6-43d6-be95-32557a74e197
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions/{{subscription_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
        - "{{subscription_id}}"
    response: []
  - name: Test Subscription
    id: 0d2cf895-8bfb-4e9d-9147-74b512c573ee
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions/{{subscription_id}}/test"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
        - "{{subscription_id}}"
        - test
    response: []
  id: 68013c31-21e3-4ebf-9dad-dec5cbea86f6
- name: Carrier Webhooks
  item:
  - name: Carrier Webhook
    id: 8b00d557-ed97-4808-9835-93e91a1b5855
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      - key: X-Carrier-Secret
        value: "{{carrier_secret_dummy}}"
      body:
        mode: raw
        raw: |-
          {
            "shipment_id": "{{shipment_id}}",
            "event_code": "picked_up"
          }
      url:
        raw: "{{base_url}}/api/v1/webhooks/carriers/dummy"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - carriers
        - dummy
    response: []
  id: 92672c4e-3c75-4175-9ddc-5505e9f67723
- name: Driver
  item:
  - name: Vehicles
    item:
    - name: Driver List Vehicles
      id: f7c652e5-5695-47ea-b29c-458ffdc1f9f7
      request:
        method: GET
        header:
        - key: Authorization
          value: Bearer {{token}}
        url:
          raw: "{{base_url}}/api/v1/driver/vehicles"
          host:
          - "{{base_url}}"
          path:
          - api
          - v1
          - driver
          - vehicles
      response: []
    - name: Driver Get Assigned Vehicle
      id: dea28d50-4442-4ce6-b307-c9b0b16fb578
      request:
        method: GET
        header:
        - key: Authorization
          value: Bearer {{token}}
        url:
          raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_id}}"
          host:
          - "{{base_url}}"
          path:
          - api
          - v1
          - driver
          - vehicles
          - "{{vehicle_id}}"
      response: []
    - name: Driver Update Assigned Vehicle
      id: 3367d1fc-0e15-4e49-b80f-9a7d2845bc41
      request:
        method: PATCH
        header:
        - key: Authorization
          value: Bearer {{token}}
        - key: Content-Type
          value: application/json
        body:
          mode: raw
          raw: |-
            {
              "vehicle_id": "{{vehicle_id}}"
            }
          options:
            raw:
              language: json
        url:
          raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_id}}"
          host:
          - "{{base_url}}"
          path:
          - api
          - v1
          - driver
          - vehicles
          - "{{vehicle_id}}"
      response: []
    - name: Driver Unassign Vehicle
      id: 736a8ef7-4e91-4b2a-9587-7026edb61e21
      request:
        method: DELETE
        header:
        - key: Authorization
          value: Bearer {{token}}
        url:
          raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_id}}"
          host:
          - "{{base_url}}"
          path:
          - api
          - v1
          - driver
          - vehicles
          - "{{vehicle_id}}"
      response: []
    - name: Driver Assign Vehicle
      id: 7dcba36d-160c-4a70-9254-072f543262be
      request:
        method: POST
        header:
        - key: Authorization
          value: Bearer {{token}}
        - key: Content-Type
          value: application/json
        body:
          mode: raw
          raw: |-
            {
              "vehicle_id": "{{vehicle_id}}"
            }
          options:
            raw:
              language: json
        url:
          raw: "{{base_url}}/api/v1/driver/vehicles"
          host:
          - "{{base_url}}"
          path:
          - api
          - v1
          - driver
          - vehicles
      response: []
    id: 3a7373a2-ac33-4f20-be48-4f36003b2e86
  - name: Driver List Bookings
    id: 0bca5d8d-32f1-4341-b4dd-2fbb71cf0d78
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/driver/bookings"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
    response: []
  - name: Driver Get Booking
    id: 92a0726d-20fa-4ea2-8b97-e51bfd2c527a
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_id}}"
    response: []
  - name: Driver Update Booking Status
    id: 712ad0b0-c5cc-4ae9-9ac5-5d025a17c46e
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "status": "picked_up",
            "note": "Collected from sender"
          }
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_id}}/status"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_id}}"
        - status
    response: []
  - name: Driver Scan Booking
    id: d42c698f-25c9-4a62-ada0-1e8df0eace9e
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "event_code": "picked_up",
            "event_description": "Package collected",
            "payload": {"scanner": "driver_app"}
          }
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_id}}/scan"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_id}}"
        - scan
    response: []
  - name: Driver Upload POD
    id: 9d5ad871-7338-41e5-b223-b4443fe1ad8c
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "file_key": "{{booking_id}}.jpg",
            "file_type": "image/jpeg",
            "signed_by": "Jane Doe"
          }
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_id}}/pod"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_id}}"
        - pod
    response: []
  - name: Driver Cancel Booking
    id: e5cecc61-df4c-4a48-a840-6e6880428caf
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      body:
        mode: raw
        raw: |-
          {
            "reason_code": "other",
            "reason": "Vehicle breakdown",
            "note": "Unable to complete pickup"
          }
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_id}}/cancel"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_id}}"
        - cancel
    response: []
  id: 5298e3e2-b06f-4fe6-855b-b6d84027c08a
- name: Vehicles
  item:
  - name: Vehicle List Vehicles
    id: 6f604504-992c-431e-8394-720337dd1af7
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
    response: []
  - name: Vehicle Get Vehicle
    id: 5bb477d5-2424-4bee-8a87-17f751d7d255
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_id}}"
    response: []
  - name: Vehicle Create Vehicle
    id: 7b9519b4-4bb8-4311-9a00-5d3c4ca04fa9
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "vehicle_type_id": "{{vehicle_type_id}}",
            "make": "Yamaha",
            "model": "MT-07",
            "color": "Black",
            "plate_number": "ABC-123",
            "vin_number": "VIN123456789",
            "engine_number": "ENG987654",
            "ref_code": "REF-001",
            "odometer": 125000,
            "year": 2022,
            "last_location_address": "1 Main Rd, Cape Town",
            "location_updated_at": "2026-02-12T10:00:00Z",
            "intergration_id": "INT-123",
            "photo_key": "vehicles/mt-07.jpg",
            "is_active": true
          }
        options:
          raw:
            language: json
      url:
        raw: "{{base_url}}/api/v1/vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
    response: []
  - name: Vehicle Update Vehicle
    id: 454c2142-bf59-4fa4-84b0-f04f5fca941c
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      body:
        mode: raw
        raw: |-
          {
            "color": "Blue",
            "last_location_address": "2 Long St, Cape Town",
            "location_updated_at": "2026-02-12T12:00:00Z"
          }
        options:
          raw:
            language: json
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_id}}"
    response: []
  - name: Vehicle Delete Vehicle
    id: 93def196-2a9b-47d2-936f-9acd571bbabf
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_id}}"
    response: []
- name: Locations
  id: 539d3dc5-9b67-45e6-bf49-a8c7de1e4fe1
  item:
  - name: Locations List Locations
    id: 412efbe2-cc0d-4b44-a5a5-ecabac74c26f
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/locations"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
    response: []
  - name: Locations Get Location
    id: 79e6e674-82b7-4306-8dc2-7dbddc8efb41
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_id}}"
    response: []
  - name: Locations Create Location
    id: 679aca02-fb29-4e79-b9b2-ab00640ad59b
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/locations"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_id}}",
            "environment_id": "{{environment_id}}",
            "name": "Warehouse A",
            "code": "WH-A",
            "company": "Acme",
            "address_line_1": "123 Main St",
            "address_line_2": "Suite 4",
            "town": "Downtown",
            "city": "Cape Town",
            "country": "ZA",
            "first_name": "John",
            "last_name": "Doe",
            "phone": "+27 11 000 0000",
            "province": "Western Cape",
            "post_code": "8001",
            "latitude": -33.9249,
            "longitude": 18.4241,
            "google_place_id": "ChIJd0N5w0ZJzR0R1bNfGdHn7Gk",
            "polygon_bounds": [
              [
                -26.2041,
                28.0473
              ],
              [
                -26.2045,
                28.048
              ],
              [
                -26.205,
                28.0478
              ]
            ]
          }
    response: []
  - name: Locations Update Location
    id: fb6ae359-d9a1-4899-85cc-c31c2dffbc59
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_id}}"
      body:
        mode: raw
        raw: |-
          {
            "name": "Warehouse A Updated",
            "phone": "+27 11 000 0001",
            "polygon_bounds": [
              [
                -26.2041,
                28.0473
              ],
              [
                -26.2045,
                28.048
              ],
              [
                -26.205,
                28.0478
              ]
            ]
          }
    response: []
  - name: Locations Delete Location
    id: 9c99ec8e-c465-4134-b45b-7c31b71cf088
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_id}}"
    response: []
- name: Tracking Providers
  id: 8e87960f-2bbf-4a16-a677-2203bfa05d1d
  item:
  - name: Activate Tracking Provider
    id: 45ef07ac-7612-4df6-bb15-6794391f2a38
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/activate"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - activate
      body:
        mode: raw
        raw: |-
          {
            "provider_id": "{{provider_id}}",
            "integration_data": {
              "api_key": "example-key",
              "enabled": true
            }
          }
    response: []
  - name: Update Tracking Provider Options Data
    id: 6ba76df3-c922-4b74-b8e8-c8400224ac38
    request:
      method: PUT
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/options_data"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - options_data
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_id}}",
            "integration_options_data": {
              "timezone": "UTC",
              "use_last_known_position": true
            }
          }
    response: []
  - name: Import Tracking Provider Vehicles
    id: 4c87db3f-dce2-478d-b985-7b0fcd7f2457
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - import_vehicles
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_id}}"
          }
    response: []
  - name: Import Tracking Provider Drivers
    id: 73f835a2-3a26-4a2d-95f3-3ea6eaf2d9fb
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_drivers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - import_drivers
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_id}}"
          }
    response: []
- name: Reports
  item:
  - name: Created Over Time
    id: 2799e85b-2213-401e-b6cd-f2a391845a4e
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/reports/created_over_time?date_range=1month"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - reports
        - created_over_time
        query:
        - key: date_range
          value: 1month
    response: []
  - name: Dashboard Stats
    id: 0b28b4bd-2415-4c5f-99eb-31114f145d4c
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/reports/dashboard_stats"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - reports
        - dashboard_stats
    response: []
  id: f8bf2122-1a8f-4c11-b73b-724408e399eb
- name: Auto Synced Endpoints
  item:
  - name: GET /api/v1/activity-logs
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/activity-logs"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - activity-logs
    response: []
  - name: GET /api/v1/admin/bookings
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/bookings"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - bookings
    response: []
  - name: POST /api/v1/admin/bookings/{{booking_uuid}}/assign-driver
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/bookings/{{booking_uuid}}/assign-driver"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - bookings
        - "{{booking_uuid}}"
        - assign-driver
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/admin/bookings/{{booking_uuid}}/unassign-driver
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/bookings/{{booking_uuid}}/unassign-driver"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - bookings
        - "{{booking_uuid}}"
        - unassign-driver
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/admin/merchants
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/merchants"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - merchants
    response: []
  - name: GET /api/v1/admin/shipments
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/shipments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - shipments
    response: []
  - name: POST /api/v1/admin/tracking-providers
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/admin/tracking-providers/{{provider_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
    response: []
  - name: GET /api/v1/admin/tracking-providers/{{provider_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
    response: []
  - name: PATCH /api/v1/admin/tracking-providers/{{provider_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - integration-form-fields
    response: []
  - name: POST /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - integration-form-fields
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - integration-form-fields
        - "{{field_uuid}}"
    response: []
  - name: GET /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - integration-form-fields
        - "{{field_uuid}}"
    response: []
  - name: PATCH /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - integration-form-fields
        - "{{field_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/admin/tracking-providers/{{provider_uuid}}/options
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/options"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - options
    response: []
  - name: POST /api/v1/admin/tracking-providers/{{provider_uuid}}/options
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/options"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - options
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - options
        - "{{option_uuid}}"
    response: []
  - name: GET /api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - options
        - "{{option_uuid}}"
    response: []
  - name: PATCH /api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/options/{{option_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - tracking-providers
        - "{{provider_uuid}}"
        - options
        - "{{option_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/admin/users
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/users"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - users
    response: []
  - name: POST /api/v1/admin/vehicle-types
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/vehicle-types"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - vehicle-types
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/admin/vehicle-types/{{vehicle_type_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/vehicle-types/{{vehicle_type_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - vehicle-types
        - "{{vehicle_type_uuid}}"
    response: []
  - name: GET /api/v1/admin/vehicle-types/{{vehicle_type_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/vehicle-types/{{vehicle_type_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - vehicle-types
        - "{{vehicle_type_uuid}}"
    response: []
  - name: PATCH /api/v1/admin/vehicle-types/{{vehicle_type_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/admin/vehicle-types/{{vehicle_type_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - vehicle-types
        - "{{vehicle_type_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/admin/webhook-deliveries
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/admin/webhook-deliveries"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - admin
        - webhook-deliveries
    response: []
  - name: GET /api/v1/bookings/{{booking_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/bookings/{{booking_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - bookings
        - "{{booking_uuid}}"
    response: []
  - name: GET /api/v1/cancel-reasons
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/cancel-reasons"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - cancel-reasons
    response: []
  - name: POST /api/v1/cancel-reasons
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/cancel-reasons"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - cancel-reasons
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/cancel-reasons/{{cancel_reason_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/cancel-reasons/{{cancel_reason_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - cancel-reasons
        - "{{cancel_reason_uuid}}"
    response: []
  - name: GET /api/v1/cancel-reasons/{{cancel_reason_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/cancel-reasons/{{cancel_reason_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - cancel-reasons
        - "{{cancel_reason_uuid}}"
    response: []
  - name: PATCH /api/v1/cancel-reasons/{{cancel_reason_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/cancel-reasons/{{cancel_reason_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - cancel-reasons
        - "{{cancel_reason_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/carriers
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/carriers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - carriers
    response: []
  - name: POST /api/v1/carriers
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/carriers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - carriers
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/carriers/{{carrier_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/carriers/{{carrier_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - carriers
        - "{{carrier_uuid}}"
    response: []
  - name: GET /api/v1/carriers/{{carrier_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/carriers/{{carrier_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - carriers
        - "{{carrier_uuid}}"
    response: []
  - name: PATCH /api/v1/carriers/{{carrier_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/carriers/{{carrier_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - carriers
        - "{{carrier_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/driver/bookings/{{booking_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_uuid}}"
    response: []
  - name: POST /api/v1/driver/bookings/{{booking_uuid}}/cancel
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_uuid}}/cancel"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_uuid}}"
        - cancel
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/driver/bookings/{{booking_uuid}}/pod
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_uuid}}/pod"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_uuid}}"
        - pod
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/driver/bookings/{{booking_uuid}}/scan
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_uuid}}/scan"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_uuid}}"
        - scan
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: PATCH /api/v1/driver/bookings/{{booking_uuid}}/status
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/driver/bookings/{{booking_uuid}}/status"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - bookings
        - "{{booking_uuid}}"
        - status
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/driver/vehicles/{{vehicle_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - vehicles
        - "{{vehicle_uuid}}"
    response: []
  - name: GET /api/v1/driver/vehicles/{{vehicle_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - vehicles
        - "{{vehicle_uuid}}"
    response: []
  - name: PATCH /api/v1/driver/vehicles/{{vehicle_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/driver/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - driver
        - vehicles
        - "{{vehicle_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/drivers
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/drivers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
    response: []
  - name: POST /api/v1/drivers
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/drivers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/drivers/{{driver_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
    response: []
  - name: GET /api/v1/drivers/{{driver_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
    response: []
  - name: PATCH /api/v1/drivers/{{driver_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/drivers/{{driver_uuid}}/vehicles
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}/vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
        - vehicles
    response: []
  - name: POST /api/v1/drivers/{{driver_uuid}}/vehicles
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}/vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
        - vehicles
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/drivers/{{driver_uuid}}/vehicles/{{vehicle_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
        - vehicles
        - "{{vehicle_uuid}}"
    response: []
  - name: PATCH /api/v1/drivers/{{driver_uuid}}/vehicles/{{vehicle_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/drivers/{{driver_uuid}}/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - drivers
        - "{{driver_uuid}}"
        - vehicles
        - "{{vehicle_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/locations/{{location_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_uuid}}"
    response: []
  - name: GET /api/v1/locations/{{location_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_uuid}}"
    response: []
  - name: PATCH /api/v1/locations/{{location_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/locations/{{location_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - locations
        - "{{location_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/location-types
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/location-types?merchant_id={{merchant_id}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - location-types
        query:
        - key: merchant_id
          value: "{{merchant_id}}"
    response: []
  - name: PATCH /api/v1/location-types
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/location-types"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - location-types
      body:
        mode: raw
        raw: |-
          {
            "merchant_id": "{{merchant_id}}",
            "types": [
              {
                "location_type_id": "{{location_type_uuid}}",
                "slug": "pickup",
                "title": "Pickup",
                "collection_point": true,
                "delivery_point": false,
                "sequence": 1,
                "icon": "map-pin",
                "color": "#16A34A",
                "default": true
              },
              {
                "slug": "dropoff",
                "title": "Dropoff",
                "collection_point": false,
                "delivery_point": true,
                "sequence": 2,
                "icon": "flag",
                "color": "#2563EB",
                "default": false
              }
            ]
          }
    response: []
  - name: DELETE /api/v1/merchants/{{merchant_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
    response: []
  - name: GET /api/v1/merchants/{{merchant_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
    response: []
  - name: PATCH /api/v1/merchants/{{merchant_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/merchants/{{merchant_uuid}}/environments
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
    response: []
  - name: POST /api/v1/merchants/{{merchant_uuid}}/environments
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
        - "{{environment_uuid}}"
    response: []
  - name: GET /api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
        - "{{environment_uuid}}"
    response: []
  - name: PATCH /api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
        - "{{environment_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}/token
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/environments/{{environment_uuid}}/token"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - environments
        - "{{environment_uuid}}"
        - token
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/merchants/{{merchant_uuid}}/invites
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/invites"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - invites
    response: []
  - name: POST /api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/resend
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/resend"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - invites
        - "{{invite_uuid}}"
        - resend
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/revoke
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/revoke"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - invites
        - "{{invite_uuid}}"
        - revoke
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/merchants/{{merchant_uuid}}/members
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/members"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - members
    response: []
  - name: DELETE /api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - members
        - "{{user_uuid}}"
    response: []
  - name: PATCH /api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - members
        - "{{user_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/merchants/{{merchant_uuid}}/members/invite
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/members/invite"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - members
        - invite
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: PATCH /api/v1/merchants/{{merchant_uuid}}/settings
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/settings"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - merchants
        - "{{merchant_uuid}}"
        - settings
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/quotes
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/quotes"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - quotes
    response: []
  - name: GET /api/v1/quotes/{{quote_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/quotes/{{quote_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - quotes
        - "{{quote_uuid}}"
    response: []
  - name: GET /api/v1/reports/created_over_time
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/reports/created_over_time"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - reports
        - created_over_time
    response: []
  - name: DELETE /api/v1/shipments/{{shipment_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
    response: []
  - name: GET /api/v1/shipments/{{shipment_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
    response: []
  - name: PATCH /api/v1/shipments/{{shipment_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/shipments/{{shipment_uuid}}/book
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/book"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - book
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/shipments/{{shipment_uuid}}/cancel
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/cancel"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - cancel
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/shipments/{{shipment_uuid}}/label
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/label"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - label
    response: []
  - name: GET /api/v1/shipments/{{shipment_uuid}}/quotes
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/quotes"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - quotes
    response: []
  - name: POST /api/v1/shipments/{{shipment_uuid}}/rebook
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/rebook"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - rebook
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/shipments/{{shipment_uuid}}/tracking
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/tracking"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - "{{shipment_uuid}}"
        - tracking
    response: []
  - name: POST /api/v1/shipments/on-demand
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/shipments/on-demand"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - shipments
        - on-demand
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/tracking-providers
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/tracking-providers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
    response: []
  - name: POST /api/v1/tracking-providers/{{provider_id}}/import_drivers
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_drivers"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - import_drivers
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/tracking-providers/{{provider_id}}/import_locations
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_locations"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - import_locations
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/tracking-providers/{{provider_id}}/import_vehicles
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_vehicles"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - import_vehicles
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: PUT /api/v1/tracking-providers/{{provider_id}}/options_data
    request:
      method: PUT
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/options_data"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - tracking-providers
        - "{{provider_id}}"
        - options_data
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: GET /api/v1/vehicle-types
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicle-types"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicle-types
    response: []
  - name: DELETE /api/v1/vehicles/{{vehicle_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_uuid}}"
    response: []
  - name: GET /api/v1/vehicles/{{vehicle_uuid}}
    request:
      method: GET
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_uuid}}"
    response: []
  - name: PATCH /api/v1/vehicles/{{vehicle_uuid}}
    request:
      method: PATCH
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/vehicles/{{vehicle_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - vehicles
        - "{{vehicle_uuid}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: POST /api/v1/webhooks/carriers/{{carrier_code}}
    request:
      method: POST
      header:
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/webhooks/carriers/{{carrier_code}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - carriers
        - "{{carrier_code}}"
      body:
        mode: raw
        raw: "{}"
    response: []
  - name: DELETE /api/v1/webhooks/subscriptions/{{subscription_uuid}}
    request:
      method: DELETE
      header:
      - key: Authorization
        value: Bearer {{token}}
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions/{{subscription_uuid}}"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
        - "{{subscription_uuid}}"
    response: []
  - name: POST /api/v1/webhooks/subscriptions/{{subscription_uuid}}/test
    request:
      method: POST
      header:
      - key: Authorization
        value: Bearer {{token}}
      - key: Content-Type
        value: application/json
      url:
        raw: "{{base_url}}/api/v1/webhooks/subscriptions/{{subscription_uuid}}/test"
        host:
        - "{{base_url}}"
        path:
        - api
        - v1
        - webhooks
        - subscriptions
        - "{{subscription_uuid}}"
        - test
      body:
        mode: raw
        raw: "{}"
    response: []
auth:
  type: bearer
  bearer:
  - key: token
    value: "{{bearer_token}}"
    type: string
variable:
- id: 52849153-f7cc-443e-b978-c0e6fc646fca
  key: base_url
  value: http://localhost
- id: c14ee1e7-0bf4-499a-af1a-49ae6d5e8c70
  key: token
  value: ''
- id: db20e9e9-e92c-4230-b2ed-5618d3214576
  key: merchant_id
  value: ''
- id: 76bac6c0-4be9-4fbd-815e-e77585cb8443
  key: shipment_id
  value: ''
- id: a7995cbc-aee2-4a70-8309-d05ffcaf4fd0
  key: quote_id
  value: ''
- id: e7e79e77-abf4-4783-853e-561b5631ab3b
  key: quote_option_id
  value: ''
- id: c289a13d-f405-42ad-95d8-b62096327127
  key: booking_id
  value: ''
- id: a9949e96-8905-4c3f-a28d-8e8b51386708
  key: user_id
  value: ''
- id: f53b527c-3c3f-4431-9d2a-5b2b2fabbdcf
  key: invite_id
  value: ''
- id: 41b65c99-10d2-4330-aedb-472b95f98c54
  key: subscription_id
  value: ''
- id: e0d1efb0-1f1f-4b38-925d-bb0c3afd6046
  key: carrier_secret_dummy
  value: change-me
- id: e395d2a9-a499-4868-8581-82358c8e1a6a
  key: merchant_uuid
  value: ''
  type: default
- id: 219aae7b-12f7-4401-8302-b115568f695c
  key: environment_id
  value: ''
  type: default
