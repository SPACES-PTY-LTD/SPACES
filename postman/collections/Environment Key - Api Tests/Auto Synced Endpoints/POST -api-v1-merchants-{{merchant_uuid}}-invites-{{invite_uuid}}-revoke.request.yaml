$kind: http-request
name: POST /api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/revoke
url: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/invites/{{invite_uuid}}/r\
  evoke"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 85000
