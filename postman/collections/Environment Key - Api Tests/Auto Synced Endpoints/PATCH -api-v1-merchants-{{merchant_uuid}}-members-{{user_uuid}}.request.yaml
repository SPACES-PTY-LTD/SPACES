$kind: http-request
name: PATCH /api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}
url: "{{base_url}}/api/v1/merchants/{{merchant_uuid}}/members/{{user_uuid}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 88000
