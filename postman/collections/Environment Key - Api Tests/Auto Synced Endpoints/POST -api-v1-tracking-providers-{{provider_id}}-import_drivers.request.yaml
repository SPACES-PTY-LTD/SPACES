$kind: http-request
name: POST /api/v1/tracking-providers/{{provider_id}}/import_drivers
url: "{{base_url}}/api/v1/tracking-providers/{{provider_id}}/import_drivers"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 108000
