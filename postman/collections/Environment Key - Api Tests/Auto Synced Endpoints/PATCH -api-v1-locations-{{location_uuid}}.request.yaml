$kind: http-request
name: PATCH /api/v1/locations/{{location_uuid}}
url: "{{base_url}}/api/v1/locations/{{location_uuid}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 67000
