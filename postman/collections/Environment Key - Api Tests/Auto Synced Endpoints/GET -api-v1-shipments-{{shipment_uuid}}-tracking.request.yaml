$kind: http-request
name: GET /api/v1/shipments/{{shipment_uuid}}/tracking
url: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}/tracking"
method: GET
headers:
  Authorization: Bearer {{token}}
order: 105000
