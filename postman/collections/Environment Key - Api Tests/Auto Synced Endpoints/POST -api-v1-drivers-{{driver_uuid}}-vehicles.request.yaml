$kind: http-request
name: POST /api/v1/drivers/{{driver_uuid}}/vehicles
url: "{{base_url}}/api/v1/drivers/{{driver_uuid}}/vehicles"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 60000
