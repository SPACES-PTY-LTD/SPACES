$kind: http-request
name: PATCH
  /api/v1/admin/tracking-providers/{{provider_uuid}}/integration-form-fields/{{field_uuid}}
url: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_uuid}}/integratio\
  n-form-fields/{{field_uuid}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 15000
