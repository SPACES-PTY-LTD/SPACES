$kind: http-request
name: POST /api/v1/webhooks/subscriptions/{{subscription_uuid}}/test
url: "{{base_url}}/api/v1/webhooks/subscriptions/{{subscription_uuid}}/test"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 123000
