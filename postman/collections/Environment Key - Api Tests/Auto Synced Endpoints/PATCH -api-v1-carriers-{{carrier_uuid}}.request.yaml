$kind: http-request
name: PATCH /api/v1/carriers/{{carrier_uuid}}
url: "{{base_url}}/api/v1/carriers/{{carrier_uuid}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 42000
