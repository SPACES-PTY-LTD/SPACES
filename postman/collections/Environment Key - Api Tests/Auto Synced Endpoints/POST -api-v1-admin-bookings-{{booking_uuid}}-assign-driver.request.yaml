$kind: http-request
name: POST /api/v1/admin/bookings/{{booking_uuid}}/assign-driver
url: "{{base_url}}/api/v1/admin/bookings/{{booking_uuid}}/assign-driver"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 3000
