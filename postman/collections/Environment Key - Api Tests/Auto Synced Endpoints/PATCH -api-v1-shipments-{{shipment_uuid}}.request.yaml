$kind: http-request
name: PATCH /api/v1/shipments/{{shipment_uuid}}
url: "{{base_url}}/api/v1/shipments/{{shipment_uuid}}"
method: PATCH
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 99000
