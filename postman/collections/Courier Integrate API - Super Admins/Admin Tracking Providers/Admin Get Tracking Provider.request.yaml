$kind: http-request
url: "{{base_url}}/api/v1/admin/tracking-providers/{{provider_id}}?merchant_id={{merchant_id}}"
method: GET
headers:
  Authorization: Bearer {{token}}
queryParams:
  merchant_id: "{{merchant_id}}"
order: 4000
