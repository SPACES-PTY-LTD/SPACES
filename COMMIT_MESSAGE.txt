Replace automatic run-map refresh with a manual refresh button

Remove one-minute polling, tab-focus refresh and viewport re-entry loads.
Add Refresh beside Geofences to reload GPS history, server-rendered run
information and enabled geofences while retaining map and replay state.
Show loading feedback and keep manual refresh available for empty maps.
Display pickup and drop-off location names above their addresses in the
run detail shipment table, with company and missing-data fallbacks.

Update the dashboard plan and release notes. Require agents to maintain
this commit-message draft after every change through AGENTS.md.

Verification: 10 replay/geofence loader tests, website TypeScript,
focused ESLint and git diff --check passed.
Live browser verification and deployment remain pending.
